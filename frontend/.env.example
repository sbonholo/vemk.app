# Beija Frontend Environment Variables

# Override the backend base URL. Leave blank when using the Vite dev proxy.
VITE_API_BASE=

# Override the Socket.io URL. Leave blank when using the Vite dev proxy.
VITE_SOCKET_URL=
